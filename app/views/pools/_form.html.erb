<%= form_for(@pool) do |f| %>
  <% if @pool.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pool.errors.count, "error") %> prohibited this pool from being saved:</h2>

      <ul>
      <% @pool.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :title %><br/>
  <%= f.text_field :title %>

  <%= f.label :visibility %><br/>
  <%= f.select(:visibility, '<option value="public">Public</option> <option value="Private"> Private </option>') %>
  <%= f.label :description %>
  <%= f.text_area :description, :cols => 30, :rows => 8%>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
